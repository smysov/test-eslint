const name = 'Max';

const fullname = (name, surname) => `${name} ${surname}`;

fullname(name, 'Ivanov');

const obj = {
	name: 'Oleg',
	surname: 'Petrov',
};

obj.job = 'Manager';

const user = {
	name: 'Ivan',
	age: 23,
};

user.age = 21;

const sum = (a, b) => a + b;

sum(3, 6);

var multiple = (a, b) => {
  return a*b
};

multiple(3, 6);