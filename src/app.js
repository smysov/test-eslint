const name = 'Max';
let number = 13;

const fullname = (name, surname, age) => `${name} ${surname} ${age}`;

fullname(name, 'Ivanov', number);

const obj = {
	name: 'Oleg',
	surname: 'Petrov',
};

obj.job = 'Manager';

const user = {
	name: 'Ivan',
	age: 23,
};

user.age = 21;

const sum = (a, b) => a + b;

sum(3, 6);

const multiple = (a, b) => a * b;

multiple(3, 6);