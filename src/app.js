const name = 'Max';

const fullname = (name, surname) => `${name} ${surname}`;

fullname(name, 'Ivanov');

const obj = {
	name: 'Oleg',
	surname: 'Petrov',
};

obj.job = 'Manager'

const user = {
	name: 'Ivan',
	age: 23,
};

user.age = 21